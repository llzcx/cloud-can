package ccw.serviceinnovation.node.secret.impl;

import ccw.serviceinnovation.node.secret.Secret;

public class Sm4SecretImpl implements Secret {

    @Override
    public byte[] encode() {
        return new byte[0];
    }

    @Override
    public byte[] decode() {
        return new byte[0];
    }
}
