package ccw.serviceinnovation.node.reedsolomon;

import ccw.serviceinnovation.node.server.db.DataDecoderHandler;

import java.io.IOException;

public class RsDecoderHandlerImpl extends DataDecoderHandler {

    @Override
    public byte[] decoderMulti(byte[][] data) throws IOException {
        return super.decoderMulti(data);
    }

    @Override
    public byte[] decoder(byte[] data) throws IOException {
        return super.decoder(data);
    }
}
