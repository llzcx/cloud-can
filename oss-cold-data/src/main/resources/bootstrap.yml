spring:
  application:
    name: oss-cold-data  # 服务名称
  profiles:
    active: dev #开发环境，这里是dev
  cloud:
    nacos:
      server-addr: localhost:8848 # Nacos地址
      config:
        file-extension: yaml # 文件后缀名
        username: nacos
        password: nacos
      discovery:
        metadata.cold_storage_name: cold-storage1
        ephemeral: true
#        namespace: public #


