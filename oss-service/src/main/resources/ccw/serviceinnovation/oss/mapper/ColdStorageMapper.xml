<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ccw.serviceinnovation.oss.mapper.ColdStorageMapper">

    <select id="get" resultType="ccw.serviceinnovation.common.entity.ColdStorage">
        select * from cold_storage where etag = #{etag}
    </select>
</mapper>
